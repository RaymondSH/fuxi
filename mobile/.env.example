# 复制成 .env 后按需修改。EXPO_PUBLIC_* 前缀的变量会暴露给客户端代码。
#
# 后端 API 根路径。
# 默认指向线上前端 :19000 的 /api 反代（Next.js rewrites 转发到 FastAPI，等同访问后端）。
# 本地开发后端时设为 http://localhost:8000/api。
EXPO_PUBLIC_API_BASE=http://118.25.93.30:19000/api
